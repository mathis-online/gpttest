# Kinder-Schach Lernprogramm

Ein kleines, offline-fähiges Schach-Lernprogramm für Kinder. Läuft komplett im Browser ohne Build-Schritt.

## Entwicklung

Öffne `public/index.html` im Browser. Tests können via `node tests/moves.spec.js` ausgeführt werden.
